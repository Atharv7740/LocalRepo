1 . stages status main ho tab

<git reset "filename">
<git reset> saare file delete;

2. commited changes 
  
  git reset HEAD ~1

3. commited changes many

git reset <-commit hash>// obatin usign <git log>

git reset --hard < commit hash> to see deletion in vs code too
